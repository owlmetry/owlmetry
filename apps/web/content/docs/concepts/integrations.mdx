---
title: Integrations
description: Connect third-party services to sync data into OwlMetry user properties.
---

Integrations connect external services to OwlMetry. They sync data from services like RevenueCat into [user properties](/docs/concepts/user-properties), giving you a unified view of each user's subscription status, revenue, and entitlements alongside their analytics data.

## How Integrations Work

Each integration is configured per-project and consists of:

1. **Provider** — the third-party service (e.g., `revenuecat`)
2. **Config** — API credentials and settings specific to that provider
3. **Webhook** — an endpoint that receives real-time updates from the provider
4. **Sync** — on-demand API calls to pull data for existing users

When a webhook fires or a sync runs, the integration maps the provider's data to OwlMetry user properties. These properties then appear in the Users list in the dashboard.

## RevenueCat

RevenueCat is the first supported integration. It syncs subscription lifecycle events into user properties.

### Properties Set

| Property | Description | Example |
|----------|-------------|---------|
| `rc_subscriber` | Whether the user has a live, renewing subscription. False for cancelled trials and cancelled paid subs even if they're still within the paid period. | `"true"` or `"false"` |
| `rc_will_renew` | Whether the subscription will auto-renew at the end of the current period. Used alongside `rc_period_type` to distinguish active trials (sky badge) from cancelled trials (red badge). | `"true"` or `"false"` |
| `rc_status` | Subscription status | `"active"`, `"trialing"`, `"cancelled"`, `"expired"`, `"billing_issue"` |
| `rc_product` | Product identifier (the RevenueCat `product_id`) | `"premium_monthly"` |
| `rc_entitlements` | Active entitlement names (comma-separated) | `"pro,premium"` |
| `rc_last_purchase` | Last purchase amount and currency | `"9.99 USD"` |
| `rc_period_type` | Whether the subscription is in trial vs fully billing | `"trial"`, `"intro"`, `"normal"`, `"promotional"` |
| `rc_billing_period` | Billing cadence of the active subscription. During a trial this reflects the trial length, not the contracted cadence. | `"weekly"`, `"monthly"`, `"yearly"`, `"lifetime"` |

### Setup

1. **Generate a RevenueCat V2 Secret API key** in the RevenueCat dashboard (Project Settings → API Keys → + New secret API key). Set both of these sections to **Read only** at the section level (use the top-right dropdown on each section):
   - **Customer information** → **Read only**
   - **Project configuration** → **Read only**

   Setting section-level Read only gives us all the sub-permissions we need today and any new ones added later. All other sections can stay **No access**.

2. **Add the integration** via dashboard (Project > Integrations > Connect RevenueCat) or CLI:
   ```bash
   owlmetry integrations add revenuecat \
     --project-id <id> \
     --api-key <revenuecat-secret-key>
   ```
   A webhook secret is auto-generated. The response (CLI, MCP, and API) includes a `webhook_setup` section with the exact values to paste into RevenueCat.

3. **Configure the webhook** in RevenueCat dashboard (Settings → Webhooks → + New Webhook) using the values from `webhook_setup`:
   - **Webhook name**: `Owlmetry` (or any name)
   - **Webhook URL**: from `webhook_setup.webhook_url`
   - **Authorization header value**: from `webhook_setup.authorization_header`
   - **Environment**: `Both Production and Sandbox`
   - **Events filter**: App = `All apps`, Event type = `All events`

4. **Backfill existing users** with the sync command:
   ```bash
   owlmetry integrations sync revenuecat --project-id <id>
   ```

### Data Flow

```
RevenueCat Purchase/Renewal/Cancellation
  -> Webhook POST to /v1/webhooks/revenuecat/:projectId
  -> Maps event to user properties (rc_subscriber, rc_status, etc.)
  -> Merges into the project-level user's properties
  -> Visible in Users list with Paid/Cancelled badges
```

### Sync

Webhooks capture future events, but users who subscribed before the integration was set up need a backfill. The sync endpoints call RevenueCat's REST API to fetch subscriber data:

- **Bulk sync** — syncs all non-anonymous users in the project (rate-limited to ~180 req/min)
- **Single-user sync** — fetches one subscriber's data immediately

Sync and webhooks also backfill **Apple Search Ads attribution** from RevenueCat's subscriber attributes (`$mediaSource`, `$campaign`, `$adGroup`, `$keyword`) for paying users. RC populates these names only for subscribers — if you want names for all attributed users (including free ones), connect the **Apple Search Ads integration** below. Existing attribution data written by either path is never overwritten (per-field merge).

## Apple Search Ads

Resolves Apple Search Ads campaign, ad group, keyword, and ad **IDs** — captured at install time by the Swift SDK's AdServices token flow — into human-readable **names** via Apple's [Campaign Management API](https://developer.apple.com/documentation/apple_ads). Complementary to (not dependent on) the RevenueCat integration: ASA resolves names for *every* attributed user, not just subscribers.

### Properties Set

| Property | Description | Example |
|----------|-------------|---------|
| `asa_campaign_name` | Human-readable campaign name | `"Holiday US Campaign"` |
| `asa_ad_group_name` | Ad group name | `"Installs - High Value"` |
| `asa_keyword` | Keyword text (if keyword-level attribution) | `"photo editor"` |
| `asa_ad_name` | Ad name (Custom Product Page ads etc.) | `"Ad Seven"` |

Numeric IDs (`asa_campaign_id`, `asa_ad_group_id`, `asa_keyword_id`, `asa_ad_id`) are captured separately by the Swift SDK at install time — see [Attribution](/docs/concepts/user-properties#reserved-namespaces). The integration never overwrites existing keys; names are additive.

### Setup

1. **Invite an API user** at ads.apple.com → Account Settings → User Management → Invite Users. Assign role `API Read Only` (or any role starting with "API").
2. **Generate an EC P-256 keypair** locally:
   ```bash
   openssl ecparam -genkey -name prime256v1 -noout -out private-key.pem
   openssl ec -in private-key.pem -pubout -out public-key.pem
   ```
3. **Upload the public key** at ads.apple.com → Account Settings → API. Apple returns three values: `clientId`, `teamId`, `keyId`.
4. **Find your `orgId`** — it's shown top-right in ads.apple.com, or you can skip this for now and use the **Test Connection** button after setup to list accessible orgs.
5. **Connect the integration** via dashboard (Integrations page → Connect Apple Search Ads) or CLI:
   ```bash
   owlmetry integrations add apple-search-ads \
     --project-id <id> \
     --client-id <SEARCHADS...> \
     --team-id <SEARCHADS...> \
     --key-id <key-id> \
     --private-key-pem "$(cat private-key.pem)" \
     --org-id <org-id>
   ```
6. **Test connection** (dashboard button or CLI `owlmetry integrations test apple-search-ads`) — validates credentials against `GET /api/v5/acls` and lists the orgs accessible to your credentials.
7. **Backfill existing users** with `owlmetry integrations sync apple-search-ads --project-id <id>` — sweeps every user with a stored `asa_campaign_id` and resolves names.

### Data Flow

```
Swift SDK AdServices capture
  -> server writes asa_*_id (numeric)
  -> fire-and-forget: call Apple Ads API to resolve names
  -> writes asa_campaign_name / asa_ad_group_name / asa_keyword / asa_ad_name
```

For users attributed before the integration was connected, the bulk sync (`apple_ads_sync` job) handles the backfill. Errors are surfaced on the integrations page via the Test Connection action.

### Heads up: API sunset

Apple has announced the v5 Campaign Management API will be sunset on **January 26, 2027**, replaced by a new **Apple Ads Platform API** shipping Summer 2026. OwlMetry will migrate customers to the Platform API ahead of the sunset.

## Managing Integrations

### Dashboard

Open the **Integrations** sidebar entry in the dashboard (`/dashboard/integrations`). The page lists every project and its configured integrations, with per-integration controls to update config, toggle enabled/disabled, sync users, or remove.

### CLI

```bash
owlmetry integrations providers                    # List supported providers
owlmetry integrations list --project-id <id>       # List configured integrations
owlmetry integrations add <provider> --project-id <id> --api-key <key>
owlmetry integrations update <provider> --project-id <id> --api-key <new-key>
owlmetry integrations remove <provider> --project-id <id>
owlmetry integrations sync <provider> --project-id <id>
owlmetry integrations sync <provider> --project-id <id> --user <userId>
```

### API

All integration endpoints are under `/v1/projects/:projectId/integrations` and require `integrations:read` or `integrations:write` permissions.

## Setup Prompt

Copy-paste this prompt to have your coding agent set up the RevenueCat integration end-to-end:

```
Set up RevenueCat integration for my OwlMetry project.

1. Use the OwlMetry CLI to add the RevenueCat integration to my project with my
   RC V2 secret API key (needs Customer information → Read only AND Project
   configuration → Read only at the section level, everything else No access).
   Use `/owlmetry-cli` for CLI syntax.
2. Show me the webhook setup values from the output so I can paste them into
   RevenueCat.
3. After I confirm the webhook is configured, run a bulk sync to backfill
   existing subscribers.
4. Use `/owlmetry-swift` or `/owlmetry-node` to add Owl.setUserProperties()
   calls after in-app purchase events so the dashboard updates immediately
   (without waiting for the webhook).
```

This prompt guides the agent through all four steps: CLI integration setup, webhook configuration, backfill sync, and client-side property updates for instant feedback.

## Adding New Providers

The integration framework uses a central provider registry in `packages/shared/src/integrations.ts`. Each provider defines its config fields (required/optional, sensitive/public) and validation rules. Adding a new provider is a matter of adding an entry to the `INTEGRATION_PROVIDERS` array.
