[Funnels](/docs/concepts/funnels) track user progression through a sequence of steps — onboarding, checkout, etc. Definitions are scoped to projects. Each step has a name and an event filter that matches on `step_name` and/or `screen_name`.
