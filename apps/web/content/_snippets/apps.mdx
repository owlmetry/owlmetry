[Apps](/docs/concepts/projects-and-apps) represent a specific platform build within a project. Each app has a platform (`apple`, `android`, `web`, `backend`) and an optional bundle identifier — required for non-backend platforms, immutable after creation. Creating an app auto-generates a `client_secret` API key for SDK use.
