[Feedback](/docs/concepts/feedback) is free-text input submitted by end users via the Swift SDK's `OwlFeedbackView` / `Owl.sendFeedback` API or the Node SDK's `Owl.sendFeedback`. Each submission carries device info, app version, environment, country, and a session ID linking back to the user's session timeline.
