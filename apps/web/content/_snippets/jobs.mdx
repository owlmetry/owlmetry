[Background jobs](/docs/concepts/background-jobs) are asynchronous server-side tasks with progress tracking, used for long-running operations like bulk data syncs. Only one instance of each job type per project can run at a time.
