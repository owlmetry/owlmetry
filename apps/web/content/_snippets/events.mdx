[Events](/docs/concepts/events) are the raw log records emitted by SDKs — every `Owl.info()`, `Owl.error()`, `Owl.step()`, etc. Filter by app, level, user, session, environment, screen, or time range. Defaults to the last 24 hours.
