[Issues](/docs/concepts/issues) are error events automatically grouped by fingerprint. Each issue has a status (`new` / `in_progress` / `resolved` / `silenced` / `regressed` / `snoozed`), a list of occurrences (affected sessions), comments, and version tracking for regression detection (semver-aware: `1.10.0 > 1.9.0`).
