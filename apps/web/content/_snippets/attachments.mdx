[Event attachments](/docs/concepts/attachments) are binary files (logs, screenshots, etc.) that SDKs optionally upload alongside an error event. Each project has a storage quota (default 5 GB) and each end-user has their own bucket within the project (default 250 MB).
